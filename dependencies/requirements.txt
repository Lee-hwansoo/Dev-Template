# =============================================================================
# dependencies/requirements.txt — the Python fallback `uvs` uses when
# src/pyproject.toml is absent.
#
# Note: This file is ignored if pyproject.toml and uv.lock are present.
# List additional Python packages required for the project here.
#
# Example:
# =============================================================================
# numpy==1.26.4
# opencv-python-headless
# pandas
